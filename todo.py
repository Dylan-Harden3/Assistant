# add docs for setting up auth with google
# make the tools more fluent with the system prompt (abstract class?)
# make it work with llama.cpp or other local llm
# make sure there is types in all functions
# add steps to show the LLMs actions
